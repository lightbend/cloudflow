Generates the Kustomize configuration files, in which the actual parameters values should be replaced, and then run in `kustomize`.

The following command generates the files in the `kustomize` folder.

```bash
make generate-yaml
```