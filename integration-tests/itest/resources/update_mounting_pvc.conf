cloudflow.runtimes.akka.kubernetes.pods.pod {
  volumes {
    bar {
      pvc {
        name = myclaim
        readOnly = false
      } 
    }
  }
  containers.container {
    volume-mounts {
      bar {
        mountPath: "/tmp/some-akka"
        readOnly =  false
      }
    }
  }
}
cloudflow.runtimes.spark.kubernetes.pods.pod {
  volumes {
    bar {
      pvc {
        name = myclaim
        readOnly = false
      } 
    }
  }
  containers.container {
    volume-mounts {
      bar {
        mountPath: "/tmp/some-spark"
        readOnly =  false
      }
    }
  }
}

cloudflow.runtimes.flink.kubernetes.pods.pod {
  volumes {
    bar {
      pvc {
        name = myclaim
        readOnly = false
      } 
    }
  }
  containers.container {
    volume-mounts {
      bar {
        mountPath: "/tmp/some-flink"
        readOnly =  false
      }
    }
  }
}