#tag::all[]
blueprint {
#tag::streamlets[]
  streamlets {
    sensor-data = com.example.SensorDataIngress
    metrics = com.example.SensorDataToMetrics
    validation = com.example.MetricsValidation
    reporter = com.example.MetricsEgress
  }
#end::streamlets[]

#tag::connections[]
  topics {
    sensor-data.connections = [sensor-data.out, metrics.in]
    metrics.connections = [metrics.out, validation.in]
    validated.connections = [validation.out-0, reporter.in]
  }
#end::connections[]
}
#end::all[]
